server:
  port: 8081
  servlet:
    context-path: /api/v1

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/emailparsing
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: parsing

#  liquibase:
#    enabled: true
#    change-log: classpath:db/changelog/db.changelog-master.xml
#    default-schema: parsing

logging:
  level:
    liquibase: DEBUG